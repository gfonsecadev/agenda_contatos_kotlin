<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="@color/main_orange"> <!-- cor a que a ser mostrada com ondulação no ripple -->
    <!-- Camada de máscara para limitar o ripple -->
    <item android:id="@android:id/mask">
        <shape >
            <corners android:radius="20dp"/>
            <solid android:color="#872202"/>
        </shape>
    </item>
</ripple>
