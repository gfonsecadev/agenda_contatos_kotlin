<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.MinhaAgenda" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/teal_200</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">#F63E03</item>
        <!-- Customize your theme here. -->
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:navigationBarColor">#EEF3683C</item>
    </style>

    <style name="Theme.MinhaAgenda.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.MinhaAgenda.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.MinhaAgenda.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="Theme.MinhaAgenda.CustomDialog" parent="Theme.AppCompat.Dialog">
        <!-- Define um estilo personalizado chamado Theme.MinhaAgenda.CustomDialog que herda de Theme.AppCompat.Dialog.
             Isso significa que o estilo base será o de um diálogo compatível com a biblioteca AppCompat. -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!-- Define o fundo da janela do diálogo como transparente. Isso é útil para permitir que o layout personalizado
             com cantos arredondados seja exibido corretamente sem interferência de um fundo padrão. -->
        <item name="android:windowNoTitle">true</item>
        <!-- Remove o título padrão do diálogo. Isso é útil se você quiser usar um layout completamente personalizado
             e não precisar de um título padrão. -->
        <item name="android:windowIsFloating">true</item>
        <!-- Define o diálogo como uma janela flutuante, ou seja, ele aparecerá acima de outras janelas e terá uma
             borda flutuante padrão. Isso é típico para diálogos que se destacam do conteúdo principal. -->
        <item name="android:backgroundDimEnabled">false</item>
        <!-- Desativa o sombreamento de fundo quando o diálogo é exibido. Normalmente, quando um diálogo é mostrado,
             o fundo é escurecido para focar a atenção no diálogo. Definir isso como false remove esse efeito de escurecimento. -->
    </style>

</resources>